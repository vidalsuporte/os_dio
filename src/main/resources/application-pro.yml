spring:
    dataource:
      ulr: jdcb:postgreql://${PGHOST}:${PGPORT}/${PGDATABASE}
      username: ${PGUSER}
      password: ${PGPASSWORD}

    jpa:
      open-in-view: false
      hibernate:
        dll-auto: create


